import { Layout } from "antd";
import React, { FC } from "react";

const SettingsPage: FC = () => {
  return <Layout className="site-layout layout_m-300">Настройки</Layout>;
};

export default SettingsPage;
