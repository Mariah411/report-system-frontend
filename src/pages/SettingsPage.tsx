import { Layout } from "antd";
import React, { FC } from "react";

const SettingsPage: FC = () => {
  return <Layout className="site-layout layout_m">Настройки</Layout>;
};

export default SettingsPage;
