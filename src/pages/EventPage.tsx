import { Layout } from "antd";
import React, { FC } from "react";

const EventPage: FC = () => {
  return (
    <Layout
      className="site-layout"
      style={{
        marginLeft: 300,
        textAlign: "center",
      }}
    >
      Мероприятия
    </Layout>
  );
};

export default EventPage;
